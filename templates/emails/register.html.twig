{# templates/emails/signup.html.twig #}
{% apply inline_css %}
    <style>
        .content {
            width: 100%;
            height: 100%;
            background-color: #19191b;
            padding-top: 30px;
        }

        .title {
            font-weight: bold;
            font-size: 3rem;
            margin: 20px auto;
            width: fit-content;
            color: rgb(86, 158, 241);
        }

        .hint {
            margin: 20px auto;
            font-weight: lighter;
            color: rgb(184, 184, 184);
            font-size: 14px;
            text-align: center;
        }

        .code-back {
            position: relative;
            width: 500px;
            margin: 0 auto;
            height: 400px;
            border-radius: 10px;
            background: #202124;
            padding-top: 50px;
        }

        .code {
            font-size: 28px;
            font-weight: 400;
            color: #3ed500;
            background: #3b3b3b;
            font-style: normal;
            width: fit-content;
            margin: 0 auto;
            padding: 20px 50px;
            border-radius: 12px;
        }

        .ps {
            color: rgb(184, 184, 184);
            font-size: 12px;
            font-weight: lighter;
            position: absolute;
            left: 30px;
            bottom: 40px;
        }

        .your-code {
            font-weight: bold;
            color: whitesmoke;
            padding-left: 40px;
            margin-bottom: 30px;
            font-size: 20px;
        }

        .warn {
            color: rgb(184, 184, 184);
            font-size: 14px;
            font-weight: lighter;
            text-decoration: underline;
            margin-top: 30px;
            text-align: center;
        }
    </style>
    <div class="content">
        <div class="title">Welcome PHP MALL!</div>
        <div class="hint">您正在注册该邮箱，请使用下方验证码进行验证，如果不是您的行为请忽略该邮件:</div>
        <div class="code-back">
            <div class="your-code">你的验证码：</div>
            <div class="code">{{ code }}</div>
            <div class="warn">验证码将会在三分钟后过期，请尽快使用！</div>
            <div class="ps">PS:请勿回复该邮件！</div>
        </div>
    </div>
{% endapply %}

